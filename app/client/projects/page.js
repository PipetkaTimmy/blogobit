import styles from "./projects.module.css";

const projects = () => {
  return (
    <main className="container">
      <div className="staticBlock">Проекты</div>
    </main>
  );
};

export default projects;
